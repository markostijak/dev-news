import { GItHubLoginProvider } from './git-hub-login-provider';

describe('GItHubLoginProvider', () => {
  it('should create an instance', () => {
    expect(new GItHubLoginProvider()).toBeTruthy();
  });
});
